{% extends 'AppBundle::base.html.twig' %}

{% block body %}
<div class="container">
    <a href="{{ url('tags') }}" title="tags">&lt;- {{ 'list.back'|trans }}</a>
    <h2>{{ 'delete'|trans }} {{ 'tag'|trans }}: {{tag.name}}</h2>
    <div>{{ form_errors(form) }}</div>
    {{ form(form, {'method': 'POST'}) }}
</div>
{% endblock %}