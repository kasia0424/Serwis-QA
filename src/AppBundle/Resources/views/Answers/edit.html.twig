{% extends 'AppBundle::base.html.twig' %}

{% block body %}
<div class="container">
    <a href="{{ url('answers') }}" title="answers">&lt;- {{'list.back'|trans}}</a>
    <h2>{{'edit'|trans}} {{'answer'|trans}}</h2>
    <div>{{ form_errors(form) }}</div>
    {{ form(form, {'method': 'POST'}) }}
</div>
{% endblock %}